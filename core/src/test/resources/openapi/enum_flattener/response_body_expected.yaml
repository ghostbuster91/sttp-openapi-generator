openapi: 3.0.3
info:
  title: Entities
  version: "1.0"
servers:
  - url: /
paths:
  /:
    get:
      operationId: getRoot
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GetRoot"
components:
  schemas:
    GetRoot:
      type: string
      enum:
        - a
        - b
