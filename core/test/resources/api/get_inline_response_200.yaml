openapi: 3.0.3
info:
  title: Entities
  version: "1.0"
paths:
  /person:
    get:
      tags:
        - person
      operationId: getPersonById
      responses:
        "200":
          description: successful operation
          content:
            application/json:
              schema:
                required:
                  - status
                type: object
                properties:
                  status:
                    type: integer
